{
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "bash \"$(git rev-parse --show-toplevel 2>/dev/null || pwd)/setup.sh\" 2>/dev/null || true"
      }
    ]
  }
}
